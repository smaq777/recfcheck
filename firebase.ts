/**
 * Firebase configuration - DEPRECATED
 * 
 * This file is kept for reference only.
 * Authentication is now handled via Neon PostgreSQL + Resend API
 * See neon-auth.ts for production authentication implementation
 */

export const firebaseConfig = {
  // Firebase config removed - use Neon instead
};

console.warn('[Firebase] Firebase integration removed. Using Neon PostgreSQL for authentication.');
